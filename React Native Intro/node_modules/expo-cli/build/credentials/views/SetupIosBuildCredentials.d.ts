import { Context, IView } from '../context';
interface CliOptions {
    nonInteractive?: boolean;
    appleId?: string;
}
interface SetupIosBuildCredentialsOptions extends CliOptions {
    experienceName: string;
    bundleIdentifier: string;
}
export declare class SetupIosBuildCredentials implements IView {
    private options;
    constructor(options: SetupIosBuildCredentialsOptions);
    open(ctx: Context): Promise<IView | null>;
    bestEffortAppleCtx(ctx: Context): Promise<void>;
}
export {};
