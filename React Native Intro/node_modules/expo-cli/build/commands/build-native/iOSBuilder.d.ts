import { Job } from '@expo/build-tools';
import { Builder, BuilderContext } from './build';
declare class iOSBuilder implements Builder {
    readonly ctx: BuilderContext;
    private credentials?;
    private buildProfile;
    constructor(ctx: BuilderContext);
    prepareJobAsync(archiveUrl: string): Promise<Job>;
    ensureCredentialsAsync(): Promise<void>;
    private prepareJobCommonAsync;
    private prepareGenericJobAsync;
    private prepareManagedJobAsync;
    private shouldLoadCredentials;
}
export default iOSBuilder;
