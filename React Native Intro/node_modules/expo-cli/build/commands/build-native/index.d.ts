import { Command } from 'commander';
interface Options {
    platform: 'android' | 'ios' | 'all';
    skipCredentialsCheck?: boolean;
    wait?: boolean;
    profile: string;
}
export declare function buildAction(projectDir: string, options: Options): Promise<void>;
export default function (program: Command): void;
export {};
